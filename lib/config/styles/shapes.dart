import 'package:flutter/material.dart';
import 'package:sign_language/config/styles/constants.dart';

class Shapes{
  static RoundedRectangleBorder rec= const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(Radius.circular(diameter))
  );
}